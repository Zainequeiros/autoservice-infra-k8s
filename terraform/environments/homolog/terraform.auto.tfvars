# Versionado — carregado automaticamente pelo Terraform (local e CI).
# Para overrides locais sem commit, use terraform.tfvars (gitignored).

enable_datadog_agent = true
dd_site              = "datadoghq.com"
